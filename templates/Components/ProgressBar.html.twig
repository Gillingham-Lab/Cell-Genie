<div{{ attributes }}>
    <div class="progress" role="progressbar" aria-valuenow="{{ current }}" aria-valuemax="{{ max }}">
        <div class="progress-bar bg-primary" style="width: {{ current/max*100|round }}%;">
            {% if showNumbers %}{{ current }}/{{ max }}{% endif %}
        </div>
    </div>
</div>