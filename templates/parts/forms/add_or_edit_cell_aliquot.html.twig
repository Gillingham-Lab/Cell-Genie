{% extends 'parts/forms/base_form.html.twig' %}
{% import 'macros.html.twig' as macros %}
{% import 'layout.html.twig' as layout %}
{% import 'utility/icons.html.twig' as icon %}

{% block title %}Gin :: {{ cell.cellNumber ?? cell.cellName }} :: {{ aliquot is not null ? "Edit Aliquot" : "New Aliquot" }}{% endblock %}

{% block form_header %}
    {{ layout.header(aliquot is not null ? "Edit" : "New", aliquot ? aliquot : "Aliquot", false, icon.any(cell, true)) }}

    {{ layout.toolbox([
        {
            "type": "up",
            "target": returnTo,
            "tooltipText": "Back",
        },
    ]) }}
{% endblock %}