{% extends 'parts/forms/base_form.html.twig' %}
{% import 'macros.html.twig' as macros %}
{% import 'layout.html.twig' as layout %}
{% import 'utility/icons.html.twig' as icon %}

{% block title %}Gin :: {{ cell is not null ? cell.cellNumber ~ " :: Edit" : "New" }}{% endblock %}

{% block form_header %}
    {{ layout.header(cell is not null ? "Edit" : "New", cell ? cell : "Cell", false, icon.any(cell)) }}

    {{ layout.toolbox([
        {
            "type": "up",
            "target": returnTo,
            "tooltipText": "Back",
        },
    ]) }}
{% endblock %}