{% extends 'parts/forms/base_form.html.twig' %}
{% import 'macros.html.twig' as macros %}
{% import 'layout.html.twig' as layout %}
{% import 'utility/icons.html.twig' as icon %}

{% block title %}Gin :: {{ title }}{% endblock %}

{% block form_header %}
    {% if createLot is defined %}
        {{ layout.header(substance ? "Edit Lot" : "New Lot", substance ? substance.shortName : typeName, false, icon.any(substance)) }}
    {% else %}
        {{ layout.header(substance ? "Edit" : "New", substance ? substance.shortName : typeName, false, icon.any(substance)) }}
    {% endif %}

    {{ layout.toolbox([
        {
            "type": "up",
            "target": returnTo,
            "tooltipText": "Back",
        },
    ]) }}
{% endblock %}