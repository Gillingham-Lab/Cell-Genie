{% extends 'parts/forms/base_form.html.twig' %}
{% import 'macros.html.twig' as macros %}
{% import 'layout.html.twig' as layout %}
{% import 'utility/icons.html.twig' as icon %}

{% block title %}Gin :: {{ title }}{% endblock %}

{% block form_header %}
    <twig:PageHeader :title="title" icon="{{ icon }}" :barcode="true"  />

    {# {% if createLot is defined %}
        {{ layout.header(substance ? "Edit Lot" : "New Lot", substance ? substance.shortName : typeName, false, icon.any(substance, true)) }}
    {% else %}
        {{ layout.header(substance ? "Edit" : "New", substance ? substance.shortName : typeName, false, icon.any(substance, true)) }}
    {% endif %}

    {{ layout.toolbox([
        {
            "type": "up",
            "target": returnTo,
            "tooltipText": "Back",
        },
    ]) }}#}
{% endblock %}